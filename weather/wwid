def calculate_herfindahl_index(df, var):
    freq = df[var].value_counts(normalize=True)
    hhi = (freq ** 2).sum()
    return hhi

# Example usage
hhi_crr = calculate_herfindahl_index(df1, 'CRR')
print(f"Herfindahl Index for CRR: {hhi_crr:.4f}")
